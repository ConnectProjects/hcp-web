# Seed script secrets — never commit
lead-finder/.env
lead-finder/node_modules/

# Local-only regression harnesses. These run against COPIES of real MasterDB
# data (worker health records) and must never enter git — neither the harness
# nor any .sqlite / packet copies it points at. See local-tests/README.md.
local-tests/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/settings.json

lead-finder/supabase/.temp/
supabase/.temp/
